# Откат обработки Alt и новый лог клиента

По запросу пользователя удалено последнее исправление оконной обработки Alt. `SeasonRuntime` восстановлен из резервной копии до этого изменения; удалены WindowsKeyMenuGuard, WindowsAltMenuProtection и связанные проверочные инструменты. Исходники эксперимента и оба его архива сохранены в `artifacts/backups/rollback-alt-20260918`.

Собранная DVSeasons.dll побайтно совпадает с предыдущей сборкой до исправления Alt:

`SHA256 5FF129DF259B84EBB053B05AAC8DCD6D9A4D66629F209C0D66CB89E6FB375A35`

Версия остаётся 0.3.3. Прежние исправления загрузки текстур и отрисовки снега сохраняются. Сборка без ошибок и предупреждений, 262 автоматических теста и 48 переводов прошли.

## Что показывает Player (2).log

- Это полноценная клиентская сессия Multiplayer 0.1.16.0, мир синхронизирован в 06:47:41.741.
- Последнее исправление действительно было установлено: подключение защиты записано на строке 340, подавление команды меню — на 347, ещё до подключения к серверу.
- Несмотря на это, есть длительные паузы кадра 15.245, 16.104 и 50.105 секунды. Синхронное чтение сезонных текстур не использовалось; измеренные участки Seasons в тяжёлом окне занимают миллисекунды.
- В 06:49:05.669 клиент отключается с PeerNotFound, затем возвращается в главное меню. Исключение Bolt.SceneVariables возникает при выгрузке мира после отключения и не устанавливает причину исходного зависания.
- Пользователь уточнил: достаточно одного нажатия Alt; перед зависанием появляется список игроков. Это переносит проверку на активированный режим мыши и GUI мультиплеера. Сам лог не содержит точного стека остановившегося потока.

Следующая проверка для локализации: на клиенте временно выключить в настройках Multiplayer `Show Player List` и повторить одиночный Alt. Это проверка гипотезы, а не установленная причина или исправление. Постоянное удаление списка игроков и новые перехваты Windows в откат не включены.

Последующий результат: отключение списка игроков не помогло; зависание воспроизводится и снаружи кабины. Дальнейший аудит нашёл цикл обновления штатного погодного интерфейса из-за запрета сброса клиентских overrides. Описание нового исправления и его проверок — в [ALT_WEATHER_REFRESH_2026-09-18.md](ALT_WEATHER_REFRESH_2026-09-18.md). Приведённый выше SHA256 относится к сохранённой сборке после отката, до исправления погодного интерфейса.
